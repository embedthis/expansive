{
    transforms: {
        md: {
            script: `
                expansive.plugin('md', function (data, file, meta) {
                    let marked = meta.control.directories.paks.join('exp-marked/node_modules/marked/bin/marked')
                    return Cmd.run(marked, {}, data)
                })
            `
        }
    }
}
