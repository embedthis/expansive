{
    transforms: {
        bash: {
            script: `
                expansive.plugin('bash', function (data, file, meta) {
                    return Cmd.sh(data)
                })
            `
        }
    }
}
